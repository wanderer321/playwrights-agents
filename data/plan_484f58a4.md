Based on the project metadata provided (`pure-activity`, Native framework, 252 pages), this is a large-scale application likely focused on event management, marketing activities, or a booking platform.

Since I do not have access to the specific source code content (like `app.json` page paths), I have designed this test plan based on the inferred business logic of an "Activity/Event" platform and standard WeChat Mini Program architectures.

```markdown
# Test Plan: pure-activity

## Overview
- **Project Name**: pure-activity
- **Framework**: WeChat Native (原生)
- **Scale**: Large (252 Pages, 5 Components)
- **Description**: A comprehensive activity management platform. Given the name and scale, the application likely supports activity publishing, browsing, registration/ticketing, user interaction, and order management.
- **Test Tool**: Minium (Python-based automation framework for WeChat Mini Programs)

## Test Strategy
Given the large number of pages (252), this test plan prioritizes core business flows (Happy Paths) and WeChat-specific features. The suites are organized by functional modules inferred from standard application architecture.

---

## Suite: 01_User_Authentication
Focus on WeChat specific login flows and user identity management.

### TC-AUTH-001: WeChat Silent Login
- **Steps**:
  1. Launch the Mini Program.
  2. Wait for the home page to load.
  3. Check if the user state is logged in (check for user avatar/nickname presence or API response).
- **Expect**: The app obtains `openid`/`token` silently via `wx.login` without interrupting the user.

### TC-AUTH-002: User Profile Authorization (GetUserInfo)
- **Steps**:
  1. Navigate to the "My Profile" or "Personal Center" page.
  2. Click the "Avatar" or "Login" button.
  3. In the Minium test, mock or handle the `wx.getUserProfile` or `button open-type="chooseAvatar"` popup.
  4. Confirm authorization.
- **Expect**: User avatar and nickname are updated successfully on the UI.

### TC-AUTH-003: Mobile Number Binding
- **Steps**:
  1. Go to Account Settings or Security Center.
  2. Click "Bind Phone Number".
  3. Trigger the `open-type="getPhoneNumber"` button.
  4. Handle the system popup (Mock agree in test environment).
- **Expect**: Mobile number is retrieved and bound to the user account; UI shows "Bound".

---

## Suite: 02_Home_and_Discovery
Focus on content display and navigation for the main landing pages.

### TC-HOME-001: Home Page Rendering
- **Steps**:
  1. Launch app to Home Page.
  2. Verify the presence of key elements: Search bar, Banner swiper, Activity List.
  3. Scroll down to trigger lazy loading (if applicable).
- **Expect**: All UI elements render correctly; no console errors; images load successfully.

### TC-HOME-002: Activity Search Functionality
- **Steps**:
  1. Click the Search bar on the Home page.
  2. Input a keyword (e.g., "Hiking").
  3. Click the "Search" button on the keyboard.
  4. Verify the list content on the result page.
- **Expect**: The result list displays activities matching the keyword; empty state is shown if no results.

### TC-HOME-003: Activity Detail Navigation
- **Steps**:
  1. Click an item in the Activity List.
  2. Wait for the Detail Page to load.
  3. Check the route path (e.g., `pages/activity/detail`).
- **Expect**: Navigation happens smoothly; Detail page displays correct ID content.

---

## Suite: 03_Activity_Registration_and_Payment
Core business logic involving WeChat Payment.

### TC-ACT-001: Activity Registration Flow (Free)
- **Steps**:
  1. On Activity Detail page, click "Sign Up" or "Register".
  2. Fill in required form fields (Name, Phone, etc.).
  3. Click "Submit".
- **Expect**: Submission succeeds; Toast message "Registration Successful"; User status changes to "Registered".

### TC-ACT-002: WeChat Payment Flow (Paid Activity)
- **Steps**:
  1. Select a paid activity.
  2. Click "Buy Now" or "Pay".
  3. Verify the call to `wx.requestPayment`.
  4. **Minium Mock**: Mock the payment success callback (as real payment requires real money/sandbox config).
  5. Verify redirection to the "Payment Success" page.
- **Expect**: Payment parameters are correct; Order status updates to "Paid" in the database/local storage.

### TC-ACT-003: Location Service (Map)
- **Steps**:
  1. On Activity Detail page, click the "Location" or "Address" area.
  2. Verify `wx.openLocation` or `wx.chooseLocation` is triggered.
  3. Check if the map component displays the correct coordinates.
- **Expect**: The map opens showing the activity venue location; Navigation options are available.

---

## Suite: 04_User_Center_and_Orders
Management of user data and past transactions.

### TC-USER-001: Order List Display
- **Steps**:
  1. Navigate to "My Orders" or "Personal Center".
  2. Verify the list of orders.
  3. Switch tabs (e.g., "Pending Payment", "Completed", "Cancelled").
- **Expect**: Orders are categorized correctly; Status labels match the business logic.

### TC-USER-002: Order Cancellation and Refund
- **Steps**:
  1. Select a "Pending Payment" or "Paid" order.
  2. Click "Cancel Order".
  3. Confirm the action.
- **Expect**: Order status updates to "Cancelled"; If paid, refund process is triggered (mocked).

### TC-USER-003: Sharing Functionality
- **Steps**:
  1. On Activity Detail page, click the "Share" button (or trigger `onShareAppMessage` via右上角菜单).
  2. Verify the share card title and image path.
- **Expect**: Share menu appears; The generated share card contains the correct activity title and cover image.

---

## Suite: 05_Performance_and_Compatibility
Due to the large page count (252), performance testing is crucial.

### TC-PERF-001: Page Rendering Performance
- **Steps**:
  1. Use Minium's performance recording capabilities.
  2. Navigate through 5-10 complex pages (e.g., long lists, heavy images).
  3. Capture metrics: `evaluateScript` time, Page load time.
- **Expect**: No page takes longer than 1.5s to render first content; CPU usage remains stable.

### TC-PERF-002: Network Exception Handling
- **Steps**:
  1. Mock network disconnection or weak network (using Minium/IDE settings).
  2. Perform an action requiring API calls (e.g., Refresh Home).
  3. Check for error toasts or fallback UI.
- **Expect**: Application handles errors gracefully; Shows "Network Error" message; Does not crash.
```