Based on the project metadata provided (Native framework, 353 pages, 5 components, named "pure-activity"), this is a large-scale WeChat Mini Program. Given the name "pure-activity", the core business logic likely revolves around activity management, event listings, or booking services.

Here is the structured test plan using Minium.

```markdown
# Test Plan: pure-activity

## Overview
- **Project Name**: pure-activity
- **Framework**: Native (原生)
- **Scale**: Large (353 Pages, 5 Components)
- **Type**: WeChat Mini Program
- **Testing Tool**: Minium

**Summary**:
Given the large number of pages (353), this test plan prioritizes core business flows and WeChat-specific features. The application appears to be activity-centric, likely involving user registration, activity browsing, booking/payment, and social sharing. The strategy focuses on verifying the "Happy Path" for key modules and ensuring stability across WeChat-specific APIs (Login, Payment, Share).

---

## Suite: 01_User_Login_And_Authorization
**Objective**: Verify user identity acquisition and WeChat authorization flows.

### TC-AUTH-001: WeChat Silent Login
- **Steps**:
  1. Launch the Mini Program for the first time (or after clearing cache).
  2. Observe the `app.js` onLaunch lifecycle.
  3. Check if the home page loads user-specific data without manual intervention.
- **Expect**: The Mini Program successfully calls `wx.login` silently, obtains the `code`, exchanges it for a `session_key`/`token` backend, and displays the home page without forcing a login popup immediately.

### TC-AUTH-002: User Profile Authorization (Avatar/Nickname)
- **Steps**:
  1. Navigate to the "User Profile" or "My Account" page.
  2. Click the "Login" or "Authorize Profile" button.
  3. If using the new API, check for the profile fill-in popup; if legacy, check for the authorization modal.
  4. Select/confirm avatar and nickname and submit.
- **Expect**: User profile information is updated successfully in the UI and synced to the backend database.

### TC-AUTH-003: Mobile Number Binding
- **Steps**:
  1. Locate a feature requiring a bound phone number (e.g., "Book Activity").
  2. Trigger the action.
  3. Click the "Get Phone Number" button (`open-type="getPhoneNumber"`).
  4. In the Minium test, mock the callback data containing the encrypted data.
- **Expect**: The system decrypts the phone number and binds it to the current user account. The UI reflects the bound status.

---

## Suite: 02_Activity_Core_Module
**Objective**: Validate the core functionality of activity browsing and details (Assuming "pure-activity" relates to events).

### TC-ACT-001: Activity List Loading and Pagination
- **Steps**:
  1. Navigate to the Activity List page.
  2. Verify the initial load (first 10-20 items).
  3. Simulate a pull-down action (`pullDownRefresh`) to reload data.
  4. Simulate scrolling to the bottom (`onReachBottom`) to trigger pagination.
- **Expect**: Data loads correctly. Pull-down refresh resets the list. Scroll-bottom successfully appends new data without duplicating existing items.

### TC-ACT-002: Activity Detail Display
- **Steps**:
  1. Click on an item in the Activity List.
  2. Verify navigation to the Detail page.
  3. Check for essential elements: Title, Time, Location, Price, Description, and "Register" button.
- **Expect**: All data renders correctly. No broken images or missing text. The "Register" button state (enabled/disabled) matches the activity status (e.g., "Not started", "Full", "Ended").

### TC-ACT-003: Activity Search and Filter
- **Steps**:
  1. Go to the Search page.
  2. Input a keyword (e.g., "Yoga") and submit.
  3. Apply filters (e.g., "This Week", "Near Me").
  4. Verify the result list.
- **Expect**: The list updates to show only activities matching the keyword and selected filters.

---

## Suite: 03_Booking_And_Payment
**Objective**: Verify the transaction flow including order creation and WeChat Payment.

### TC-PAY-001: Create Order Flow
- **Steps**:
  1. On the Activity Detail page, click "Register" or "Book Now".
  2. Select participants/tickets count.
  3. Click "Submit Order".
  4. Verify the redirection to the "Order Confirmation" page.
- **Expect**: Order is created with status "Pending Payment". Order ID is generated. UI displays correct total amount.

### TC-PAY-002: WeChat Payment Success
- **Steps**:
  1. On the Order Confirmation page, click "Pay Now".
  2. Trigger `wx.requestPayment`.
  3. In the Minium environment, simulate a successful payment callback.
  4. Verify redirection to the "Payment Success" page.
- **Expect**: Payment interface is called successfully. Order status updates to "Paid". User receives a success notification.

### TC-PAY-003: Payment Failure/Cancel Handling
- **Steps**:
  1. Initiate payment.
  2. Simulate a user cancellation or payment failure in the test environment.
  3. Check the Order Detail page status.
- **Expect**: The app handles the error gracefully (no crash). Order status remains "Pending Payment" or "Failed", allowing the user to retry payment.

---

## Suite: 04_User_Center_And_Location
**Objective**: Test user-specific features and location services.

### TC-USER-001: View My Orders
- **Steps**:
  1. Go to "My Profile" / "User Center".
  2. Click on "My Orders".
  3. Switch tabs (All, Pending, Paid, Completed).
- **Expect**: Orders are listed correctly under their respective status tabs. Clicking an order navigates to the correct detail page.

### TC-LOC-001: Location Authorization and Usage
- **Steps**:
  1. Trigger a feature requiring location (e.g., "Nearby Activities" or "Navigation").
  2. If first time, verify the system permission prompt.
  3. Allow location access.
- **Expect**: The app retrieves the user's coordinates via `wx.getLocation`. The UI updates to show nearby activities or sets the map center correctly.

### TC-LOC-002: Map Navigation
- **Steps**:
  1. Go to an Activity Detail page with a location map.
  2. Click the map component or "Navigate" button.
  3. Verify `wx.openLocation` is called.
- **Expect**: The WeChat built-in map opens showing the activity location with navigation options.

---

## Suite: 05_Sharing_And_Social
**Objective**: Validate WeChat sharing capabilities.

### TC-SHARE-001: Share to Friends (Menu)
- **Steps**:
  1. Click the top-right menu "..." on any page.
  2. Select "Forward" (转发).
  3. Verify the share card title and image (defined in `onShareAppMessage`).
- **Expect**: A share card appears with the correct title, image path, and path parameter (e.g., `/pages/detail?id=123`).

### TC-SHARE-002: Share to Timeline (Moments)
- **Steps**:
  1. Click the top-right menu "...".
  2. Select "Share to Timeline" (分享到朋友圈).
  3. Verify `onShareTimeline` configuration.
- **Expect**: The share interface opens with the correct image and query parameters.

### TC-SHARE-003: Share Button Interaction
- **Steps**:
  1. Locate a specific share button (button with `open-type="share"`) on the Activity Detail page.
  2. Click the button.
- **Expect**: The specific share dialog opens. The `onShareAppMessage` callback receives the correct parameters (e.g., specific ticket ID) allowing for deep linking.

---

## Suite: 06_Navigation_And_Performance
**Objective**: Ensure navigation stability across 353 pages.

### TC-NAV-001: TabBar Navigation
- **Steps**:
  1. Iterate through all TabBar items (e.g., Home, Activity, Profile).
  2. Click each tab rapidly.
- **Expect**: Pages switch instantly. No page stack overflow. The active tab indicator updates correctly.

### TC-NAV-002: Page Stack Depth
- **Steps**:
  1. Navigate from Home -> List -> Detail -> Booking -> Order.
  2. Click the back button repeatedly.
- **Expect**: Navigation history is preserved. Back button works correctly. No memory leaks or crashes during navigation.

### TC-PERF-001: Cold Start Performance
- **Steps**:
  1. Kill the Mini Program process.
  2. Launch via Minium script.
  3. Measure time from launch to `onReady` of the home page.
- **Expect**: The home page renders within acceptable limits (e.g., < 2 seconds) despite the project size.
```