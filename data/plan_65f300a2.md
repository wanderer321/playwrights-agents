Based on the project information provided for **pure-activity** (a native WeChat Mini Program with 53 pages and 10 components), here is the structured test plan.

**Note:** The analysis indicates `app.json` was not found/detected, which usually implies the project might be in a sub-directory or requires specific configuration loading. The test plan assumes a standard Mini Program structure for an "Activity" based application.

```markdown
# Test Plan: pure-activity

## Overview
- **Project Name**: pure-activity
- **Framework**: Native WeChat Mini Program
- **Scale**: Medium-Large (53 Pages, 10 Components)
- **Testing Tool**: Minium (Python)
- **Key Testing Focus**: 
  - User Authorization (Login)
  - Activity Lifecycle (Listing, Detail, Registration)
  - WeChat Payment integration
  - Sharing capabilities
  - Location services (if applicable to activities)

**Prerequisites**:
1. Configure Minium environment in `project.config.json`.
2. Ensure `app.json` path is correctly resolved in the test initialization script (since auto-detection failed).
3. Prepare test accounts (Organizer vs Participant) and test payment data.

---

## Suite: User Module (Login & Profile)
Focuses on user identity management and WeChat specific authorization flows.

### TC-USER-001: WeChat Silent Login
- **Steps**:
  1. Launch the Mini Program.
  2. Check current page path.
  3. Verify if user info is retrieved automatically via `wx.login` logic.
- **Expect**: App launches successfully; User is logged in silently or redirected to a welcome page without explicit errors.

### TC-USER-002: User Profile Authorization
- **Steps**:
  1. Navigate to User Profile page.
  2. If not authorized, click the "Get Avatar/Nickname" button.
  3. Mock the `wx.getUserProfile` or the new `chooseAvatar` API interaction.
  4. Verify UI updates with the new avatar/nickname.
- **Expect**: Authorization popup appears (if applicable); Profile updates successfully after user confirmation.

### TC-USER-003: Phone Number Binding
- **Steps**:
  1. Navigate to Account Settings.
  2. Click "Bind Phone Number".
  3. Trigger the `open-type="getPhoneNumber"` button event.
  4. Verify backend code decryption logic (mock or test environment).
- **Expect**: Phone number is successfully bound and displayed in settings.

---

## Suite: Activity Discovery (Home & List)
Focuses on the main entry points and data loading for activities.

### TC-HOME-001: Activity List Loading
- **Steps**:
  1. Navigate to the Home page (`pages/index/index` or similar).
  2. Wait for data loading API to complete.
  3. Check if activity list elements exist and count > 0.
- **Expect**: Activity cards are rendered; No "Empty Data" message shown (assuming active test data exists).

### TC-HOME-002: Pull-down Refresh
- **Steps**:
  1. On the Home page, simulate a pull-down gesture.
  2. Verify `onPullDownRefresh` is triggered.
  3. Check if the list data is reset and reloaded.
- **Expect**: Loading indicator appears; List refreshes with latest data.

### TC-HOME-003: Activity Search
- **Steps**:
  1. Focus on the Search input component.
  2. Input a keyword (e.g., "Hiking").
  3. Click Search button or trigger Confirm event.
  4. Verify list results match the keyword.
- **Expect**: Search results are filtered correctly; Result count matches database expectations.

---

## Suite: Activity Detail & Interaction
Focuses on the core business logic of viewing and interacting with an activity.

### TC-DETAIL-001: View Activity Detail
- **Steps**:
  1. Click on an item from the Activity List (TC-HOME-001).
  2. Verify navigation to the detail page.
  3. Check for essential elements: Title, Time, Location, Price, Organizer info.
- **Expect**: Detail page renders all information correctly; No layout breaks.

### TC-DETAIL-002: Share Activity (WeChat Feature)
- **Steps**:
  1. On the Activity Detail page, click the "Share" button (or `open-type="share"`).
  2. Verify `onShareAppMessage` return value contains correct title and path.
  3. Simulate sharing to a friend or group.
- **Expect**: Share menu appears; Shared card displays correct image and title.

### TC-DETAIL-003: Location Navigation
- **Steps**:
  1. Find the Location/Map component on the Detail page.
  2. Click to open map.
  3. Verify `wx.openLocation` is called with correct latitude/longitude.
- **Expect**: WeChat Map opens showing the activity location.

---

## Suite: Registration & Payment
Critical path testing involving WeChat Payment APIs.

### TC-PAY-001: Free Activity Registration
- **Steps**:
  1. Select a free activity (Price = 0).
  2. Click "Register" / "Join Now".
  3. Submit registration form.
- **Expect**: Success toast message; User is added to the participant list; Order status is "Confirmed".

### TC-PAY-002: Paid Activity Payment Flow
- **Steps**:
  1. Select a paid activity.
  2. Click "Pay Now".
  3. Mock `wx.requestPayment` success callback.
  4. Verify order status changes to "Paid" in the database/UI.
- **Expect**: Payment UI triggers (in test environment); Order confirmation page is displayed upon success.

### TC-PAY-003: Payment Failure Handling
- **Steps**:
  1. Initiate payment for a paid activity.
  2. Mock `wx.requestPayment` fail callback (User cancelled or Network error).
  3. Verify UI feedback and Order status.
- **Expect**: Error message displayed; Order status remains "Unpaid" or "Pending"; User can retry payment.

---

## Suite: User Center & Orders
Focuses on post-interaction data management.

### TC-UC-001: My Orders List
- **Steps**:
  1. Navigate to User Center.
  2. Click "My Orders".
  3. Verify tabs (All, Pending, Paid, Cancelled).
  4. Check data consistency with backend.
- **Expect**: Orders are listed correctly under respective tabs; Empty state shown if no orders.

### TC-UC-002: Cancel Order
- **Steps**:
  1. In "My Orders", select a "Pending/Paid" order.
  2. Click "Cancel Order".
  3. Confirm cancellation.
- **Expect**: Order status updates to "Cancelled"; Refund logic triggered (if applicable).

---

## Suite: Component Tests
Isolating the 10 custom components for UI consistency.

### TC-COMP-001: Activity Card Component Rendering
- **Steps**:
  1. Isolate the `activity-card` component (if possible via Minium component testing).
  2. Pass mock data props (Title, Image, Status).
  3. Verify computed styles based on status (e.g., "Ended" vs "Active").
- **Expect**: Component renders correctly for all status variations.

### TC-COMP-002: Loading Skeleton Component
- **Steps**:
  1. Trigger a page load with network throttling.
  2. Verify the skeleton component is visible before data loads.
- **Expect**: Skeleton screen displays during loading; Disappears once content is ready.
```